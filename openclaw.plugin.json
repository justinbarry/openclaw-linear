{
  "id": "openclaw-linear",
  "name": "Linear",
  "description": "Linear project management integration for OpenClaw",
  "skills": ["./skills"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "workspaces": {
        "type": "object",
        "description": "Named workspace configurations. Each key is a workspace name, value is the workspace config. When using multiple workspaces, tools accept a 'workspace' parameter to target a specific workspace.",
        "additionalProperties": {
          "type": "object",
          "properties": {
            "apiKey": {
              "type": "string",
              "sensitive": true,
              "description": "Linear API key for this workspace"
            },
            "webhookSecret": {
              "type": "string",
              "sensitive": true,
              "description": "Webhook signing secret for this workspace"
            },
            "teamIds": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Linear team keys to listen for (e.g. [\"ENG\", \"OPS\"]). Empty = all teams."
            },
            "eventFilter": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Event types to handle (e.g. [\"Issue\", \"Comment\"]). Empty = all types."
            },
            "agentMapping": {
              "type": "object",
              "additionalProperties": { "type": "string" },
              "description": "Map Linear user IDs to OpenClaw agent IDs for routing"
            },
            "debounceMs": {
              "type": "integer",
              "description": "Debounce window in milliseconds (default: 30000)"
            },
            "stateActions": {
              "type": "object",
              "additionalProperties": { "type": "string", "enum": ["add", "remove", "ignore"] },
              "description": "Map Linear state types or names to queue actions"
            }
          },
          "required": ["apiKey", "webhookSecret"]
        }
      },
      "apiKey": {
        "type": "string",
        "sensitive": true,
        "description": "Linear API key (legacy single-workspace config). Use 'workspaces' for multi-workspace."
      },
      "webhookSecret": {
        "type": "string",
        "sensitive": true,
        "description": "Webhook signing secret (legacy single-workspace config)"
      },
      "teamIds": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Linear team keys to listen for (legacy single-workspace config)"
      },
      "eventFilter": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Event types to handle (legacy single-workspace config)"
      },
      "agentMapping": {
        "type": "object",
        "additionalProperties": { "type": "string" },
        "description": "Map Linear user IDs to OpenClaw agent IDs (legacy single-workspace config)"
      },
      "debounceMs": {
        "type": "integer",
        "description": "Debounce window in milliseconds (default: 30000)"
      },
      "stateActions": {
        "type": "object",
        "additionalProperties": { "type": "string", "enum": ["add", "remove", "ignore"] },
        "description": "Map Linear state types or names to queue actions (legacy single-workspace config)"
      }
    }
  }
}
